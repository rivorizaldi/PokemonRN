const baseUrl = "http://192.168.1.105:3333";

const pokemonEndpoint = `${baseUrl}/pokemons`;
const userLoginEndpoint = `${baseUrl}/user/login`;
const pokemonCategoryEndpoint = `${baseUrl}/categories`;
const pokemonTypeEndpoint = `${baseUrl}/types`;

export {
    baseUrl,
    pokemonEndpoint,
    userLoginEndpoint,
    pokemonCategoryEndpoint,
    pokemonTypeEndpoint
};
